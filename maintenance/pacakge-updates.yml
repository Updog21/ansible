---
- name: Patch OS
  hosts: all
  become: true
  become_method: sudo
  tasks:
    - name: Perform package patches
      become: true
      become_method: sudo
      ansible.builtin.package:
        update_cache: yes
        name: '*'
        state: latest

